# Границы, а не точные версии: точный пин ломает деплой, если версия
# окажется недоступной, а логи Actions в этом окружении не прочитать.
# Мажорные ограничения защищают от несовместимых обновлений.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
