# Границы, а не точные версии: точный пин ломает деплой, если версия
# окажется недоступной, а логи Actions в этом окружении не прочитать.
# Мажорные ограничения защищают от несовместимых обновлений.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# Нужен FastAPI для приёма файлов (multipart/form-data).
python-multipart>=0.0.9,<1.0
# Фото пересохраняются через Pillow: это и проверка, что файл действительно
# картинка, и уменьшение до разумного размера, и побочно — снятие EXIF,
# в котором у телефонных снимков лежат GPS-координаты.
Pillow>=10.0,<13.0
