# История изменений

## Ш2 — каркас бэкенда · 2026-07-29

- `backend/` — приложение FastAPI с единственным эндпоинтом `/api/health`,
  отдающим статус, версию (SHA задеплоенного коммита) и аптайм. Версия в
  ответе нужна, чтобы отличать «сервис отвечает» от «отвечает нужный код».
- Сервис работает под отдельным системным пользователем `strapi`, исходники
  в `/opt/str-api/src`, данные в `/opt/str-api/data` — вне `/var/www/html`,
  который раздаётся наружу и чистится `--delete` при каждом деплое.
- systemd-юнит `str-api` с `Restart=always` и автозапуском; venv собирается
  на сервере, зависимости из `requirements.txt`.
- Caddy теперь маршрутизирует: `/api/*` — в uvicorn на `127.0.0.1:8000`,
  остальное — статикой. Конфигурация проверена `caddy validate` локально
  до деплоя.
- Деплой падает с логами `journalctl -u str-api`, если бэкенд не ответил на
  `/api/health` за ~50 секунд, — молчаливого «задеплоили сломанное» не будет.
- Swagger и OpenAPI-схема отключены: наружу смотрит открытый сайт.

## Ш1 — приглашение на проде как статика · 2026-07-29

- Шаблон приглашения перенесён в `site/index.html`; вёрстка, тексты и
  структура сохранены целиком.
- **Удалён код, обращавшийся к `window.storage`.** Это API среды Claude
  Artifacts, в обычном браузере его нет: отправка формы всегда падала в
  `catch` и показывала «Не получилось сохранить», а ростер оставался пустым.
  Вместо тихой ошибки — явная плашка «Запись пока закрыта» и неактивная
  кнопка. Разметка формы оставлена нетронутой, чтобы шаг 3 подключил к ней
  API без переделки вёрстки.
- Шрифты захостены локально в `site/fonts/` вместо загрузки с
  `fonts.googleapis.com`: страница не зависит от внешней сети и не мигает
  подменой шрифта.
- Anton заменён на Oswald, Bangers — на Russo One для кириллицы: у исходных
  шрифтов нет кириллических глифов, весь русский текст в них отображался
  системным шрифтом (решение Р6). Bangers оставлен для латинского
  «SMART HERO».
- Добавлены og-теги для превью ссылки в мессенджерах, favicon и
  `meta description`.
- **Исправлено при проверке в браузере:** в `fonts.css` пути были записаны
  как `url(fonts/…)`, хотя сам файл лежит внутри `fonts/` — браузер искал
  `fonts/fonts/…` и получал 404 на все 15 шрифтов. Проверка через `curl`
  этого не показывала: файлы по своим адресам существовали, ошибочной была
  ссылка из CSS. Ловится только реальным рендерингом страницы.
- Доставка на сервер переведена с base64-переменной на `rsync`: каталог со
  шрифтами вырос до ~450 КБ и перестал помещаться в окружение ssh-action,
  из-за чего деплой молча не доезжал.

## Инфраструктура — домен · 2026-07-29

- Сайт переведён на https://raikhin.duckdns.org/ (`TLS_MODE: fixed`).
  A-запись уже вела на сервер, но сертификата для этого имени у Caddy не
  было — HTTPS обрывался на рукопожатии (`tlsv1 alert internal error`).
- Обновление IP через API DuckDNS стало необязательным: шаг включается
  наличием секретов, а не режимом TLS.

## Инфраструктура — до плана · 2026-07-29

- Сайт разворачивается из `site/` пушем в `main`; nginx заменён на Caddy с
  автоматическим сертификатом Let's Encrypt. Подробности — в `CLAUDE.md`.
